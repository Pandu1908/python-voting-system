# python-voting-system
A simple Python voting system that allows voters to cast one vote for candidates A, B, or C. It prevents duplicate voting using voter IDs and stores votes with a dictionary. Users can view results or exit the system. This beginner-friendly project demonstrates sets, dictionaries, loops, conditions, user input, and validation.
